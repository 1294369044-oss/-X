---
import type { NewsItem } from "../data/news";
interface Props { item: NewsItem; compact?: boolean; }
const { item, compact = false } = Astro.props;
const time = new Intl.DateTimeFormat("zh-CN", {
  month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit",
  timeZone: "Asia/Shanghai", hour12: false,
}).format(new Date(item.publishedAt));
---
<article class:list={["news-card", { compact }]} data-category={item.category} data-search={`${item.title} ${item.summary} ${item.source}`}>
  <div class="card-meta"><span>{item.category}</span><time datetime={item.publishedAt}>{time}</time></div>
  <h3><a href={`/news/${item.slug}/`}>{item.title}</a></h3>
  {!compact && <p>{item.summary}</p>}
  <div class="card-source">来源：{item.source}</div>
</article>
